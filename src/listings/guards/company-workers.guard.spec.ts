import { CompanyWorkersGuard } from './company-workers.guard';

describe('CompanyWorkersGuard', () => {
  it('should be defined', () => {
    expect(new CompanyWorkersGuard()).toBeDefined();
  });
});
